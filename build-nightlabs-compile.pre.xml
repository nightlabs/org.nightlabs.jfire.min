<project>
	<target name="compile.pre">
		<mkdir dir="${project.dir}/${lib.dir}"/>
		
<!--
	Is loaded via remote-class-loading again.		
		<copy file="../JFireBaseClientLib/dist/JFireBaseClientLib.jar" todir="${project.dir}/${lib.dir}" failonerror="true" verbose="true"/>
-->
		<delete file="${project.dir}/${lib.dir}/JFireBaseClientLib.jar"/>

<!--
	this jar is now loaded via remote-class-loading
		<copy file="../JFireBaseDAO/dist/JFireBaseDAO.jar" todir="${project.dir}/${lib.dir}" failonerror="true" verbose="true"/>
-->
		<delete file="${project.dir}/${lib.dir}/JFireBaseDAO.jar"/>
	</target>
</project>